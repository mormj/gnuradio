/*
 * Copyright 2020 Free Software Foundation, Inc.
 *
 * This file is part of GNU Radio
 *
 * SPDX-License-Identifier: GPL-3.0-or-later
 *
 */

/* This file is automatically generated using bindtool */

#include <pybind11/pybind11.h>
#include <pybind11/complex.h>
#include <pybind11/stl.h>

namespace py = pybind11;

#include <gnuradio/channels/channel_model.h>

void bind_channel_model(py::module& m)
{
    using channel_model    = gr::channels::channel_model;


    py::class_<channel_model,gr::hier_block2,
        std::shared_ptr<channel_model>>(m, "channel_model")

        .def(py::init(&channel_model::make),
           py::arg("noise_voltage") = 0., 
           py::arg("frequency_offset") = 0., 
           py::arg("epsilon") = 1., 
           py::arg("taps") = std::vector<gr_complex>(1, 1), 
           py::arg("noise_seed") = 0, 
           py::arg("block_tags") = false 
        )
        

        .def("set_noise_voltage",&channel_model::set_noise_voltage,
            py::arg("noise_voltage") 
        )
        .def("set_frequency_offset",&channel_model::set_frequency_offset,
            py::arg("frequency_offset") 
        )
        .def("set_taps",&channel_model::set_taps,
            py::arg("taps") 
        )
        .def("set_timing_offset",&channel_model::set_timing_offset,
            py::arg("epsilon") 
        )
        .def("noise_voltage",&channel_model::noise_voltage)
        .def("frequency_offset",&channel_model::frequency_offset)
        .def("taps",&channel_model::taps)
        .def("timing_offset",&channel_model::timing_offset)
        .def("to_basic_block",[](std::shared_ptr<channel_model> p){
            return p->to_basic_block();
        })
        ;


} 
