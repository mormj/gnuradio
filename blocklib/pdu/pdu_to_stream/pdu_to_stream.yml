module: pdu
block: pdu_to_stream
label: PDU to Stream
blocktype: sync_block

parameters:
-   id: data_type
    label: Vector Data Type
    dtype: enums/data_type_t
    grc:
        default: gr.data_type_t.CF32

ports:
    - domain: message
      id: pdus
      direction: input
      optional: true

    - domain: stream
      id: out
      direction: output
      type: parameters/data_type

implementations:
    - id: cpu
# -   id: cuda

file_format: 1
