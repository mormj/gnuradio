module: bench
block: copy
label: copy
blocktype: sync_block

typekeys:
  - id: T
    type: class
    options: 
        - cf32
        - rf32
        - ri32
        - ri16
        - ri8

# Example Parameters
parameters:
-   id: use_memcpy
    label: Use Memcpy
    dtype: bool
    settable: false
    default: "false"
-   id: nproduce
    label: Num items produce
    dtype: size
    settable: false
    default: 0

# Example Ports
ports:
-   domain: stream
    id: in
    direction: input
    type: typekeys/T

-   domain: stream
    id: out
    direction: output
    type: typekeys/T

implementations:
-   id: cpu
# -   id: cuda

file_format: 1